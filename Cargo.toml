[package]
name = "rust-for-kotlin-devs"
version = "0.1.0"
edition = "2021"
description = "Runnable examples accompanying the Rust-for-Kotlin-developers guide"
license = "MIT"

[dependencies]

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
